@using EnglishNow.Web.Models.Menu
@model EnglishNow.Web.Models.Aluno.ListarViewModel;
@{
    ViewData["Title"] = "Listar Alunos - ";
    ViewData["Menu"] = Menu.Aluno;
}

<h1>Lista de Alunos</h1>

@if (Model.ExibirBotaoNovoEditar)
{
    <div class="py-2">
        <a class="btn btn-primary" asp-controller="Aluno" asp-action="Criar">Novo Aluno</a>
    </div>
}

@if (Model.Alunos == null || Model.Alunos.Count == 0)
{
    <p class="card-text">Nenhum aluno cadastrado.</p>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Nome</th>
                    <th scope="col">Email</th>
                    <th scope="col">Login</th>
                    @if (Model.ExibirBotaoNovoEditar)
                    {
                        <th scope="col"></th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (var aluno in Model.Alunos)
                {
                    <tr>
                        <th>@aluno.Id</th>
                        <td>@aluno.Nome</td>
                        <td>@aluno.Email</td>
                        <td>@aluno.Login</td>
                        @if (Model.ExibirBotaoNovoEditar)
                        {
                            <td><a class="link-underline link-underline-opacity-0" asp-controller="Aluno" asp-action="Editar" asp-route-id="@aluno.Id">Editar</a></td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
}